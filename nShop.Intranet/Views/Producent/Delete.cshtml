@model nShop.Data.Data.Sklep.Producent

@{
    ViewData["Title"] = "Usuń Producenta";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center">Usuń Producenta</h1>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h3>Czy na pewno chcesz usunąć tego producenta?</h3>
            <hr />
            <dl class="row">
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Nazwa)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Nazwa)</dd>
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Opis)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Opis)</dd>
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.SciezkaLogo)</dt>
                <dd class="col-sm-8">
                    <img src="@Model.SciezkaLogo" alt="Logo producenta" class="img-fluid" />
                </dd>
            </dl>
            <form asp-action="Delete" method="post" style="text-align: center;">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="Usuń" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-warning">Powrót do listy</a>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
